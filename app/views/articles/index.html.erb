<h1>All articles</h1>

<ul id = "articles">
	<% @articles.each do |article|%>
		<li>
			<%= link_to article.title, article_path(article)%>
		</li>
	<%end%>
</ul>
<br/>
<%= link_to "Create a New Article", new_article_path, class: "new_article" %>
<%= link_to "Browse Articles by Tag", tags_path %>

